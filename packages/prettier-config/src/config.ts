import type { RequiredOptions } from 'prettier'

const config = {
    printWidth: 120,
    semi: false,
    singleQuote: true,
    tabWidth: 4,
    trailingComma: 'all',
    arrowParens: 'avoid',
} as RequiredOptions

export = config
